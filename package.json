{
  "name": "arkon-slack",
  "version": "1.0.0",
  "description": "ARKON DevOps Slack Agent — connects Slack to the ARKON Engine via MCP",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts",
    "test": "jest --passWithNoTests",
    "ci": "npm run build && npm test -- --runInBand && npm audit --omit=dev"
  },
  "dependencies": {
    "@slack/bolt": "^4.4.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@slack/types": "^2.21.1",
    "@types/jest": "^29.5.14",
    "@types/node": "^20.14.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.4",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
